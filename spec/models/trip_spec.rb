require 'rails_helper'

# RSpec.describe Station, :type => :model do

#   let(:trip) { User.create(bike_id: 1)}
#   let(:issue) {issue = Issue.create(name: "Big Issue")}

#   describe "has a start time that converts to a date" do
#     it { expect(trip.start_time).to eq([])}
#   end
# end